Image assets for the walkthrough. The HTML and markdown reference these exact filenames:

  01-observability-agent-v2.png
  02-k8s-troubleshooter-agent.png
  02-k8s-troubleshooter-agent-2.png   (Step 3 second figure; from "K8s Troubleshooter agent with EKS MCP tools attached - 2.png")
  03-workflow-tool-parent-agent.png
  04-service-map-or-errors.png   (Step 1; maintain from "Elastic Observability service map - All good.png")
  05-eks-mcp.png                 (Step 2; maintain from "EKS MCP.png")
  05-workflow.png                (Step 5; Kibana Workflows UI with k8s_troubleshooter YAML)
  06-demo-services-service-map-or-errors.png   (Step 6; maintain from "Elastic Observability service map or error rate view for demo services.png")

Place files here; the standalone page is ../elastic-ai-agent-builder-blog-standalone.html
and loads them via chained-agents-eks-mcp/images/<filename>.

Longer-named PNGs in this folder are optional source copies from capture tools; the
numbered files above are what the page uses — keep them in sync when you update shots.

Redact tokens, account IDs, and internal hostnames before publishing.
